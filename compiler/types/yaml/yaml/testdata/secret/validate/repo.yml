secrets:
  # Implicit native secret definition.
  - name: foo

  # Declarative repository secret definition.
  - name: foob
    key: github/ocotocat/foob
    engine: native
    type: repo
  - name: foo_bar
    key: github/ocotocat/foo/bar
    engine: native
    type: repo    