steps:
  - name: sample
    template:
      name: golang
      vars:
        image: golang:latest
        pull_policy: "pull: true"
        ruleset:
          event: [push, pull_request]
          branch: [master]
