# yaml-language-server: $schema=https://github.com/go-vela/server/releases/latest/download/schema.json

version: "1"

metadata:
  auto_cancel:
    invalid_key: true

steps:
  - name: test
    image: alpine
    commands:
      - echo "Hello World"
