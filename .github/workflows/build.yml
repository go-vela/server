name: Build Action
on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: golang:latest
    steps:

    - name: clone
      uses: actions/checkout@v1

    - name: build
      run: make build
